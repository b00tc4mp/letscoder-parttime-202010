var numbers = [1, 2, 3, 4,] //original
var newArray = []
function filterMethod (callback) {
    console.log(array.filterMethod((element) == element > 1))
    var newArray = []
    
    for (var i = 0; i < array.length; i++){
        if(callback(this[i])) {
            newArray.push(this[i]);
        }
    }
    return newArray //expected [2, 3, 4]
    

} 